insert into darrun_1(kanji,HIRAGANA,mean,shape,time_ck)
values('한자','히라가나','의미','동사',SYSTIMESTAMP);

// 데이터 입력 쿼리

CREATE TABLE darrun_1(
kanji varchar2(20) not null,
hiragana varchar2(20) not null,
mean varchar2(20),
shape varchar2(20),
times TIMESTAMP NOT NULL);

테이블 생성

search 

관리자 테이블만 검색 => 관리자가 너무 많은 데이터를 입력해 넣어햐 하며, 의미가 하나 뿐만 아니라 여러 의미까지 다 넣어야 한다.

사용자 테이블 모두 검색 => 알고리즘이 복잡, 아무 데이터나 집어 넣어버리면 하나하나 지워줘야된다.

SEARCH 버튼을 누르면 

유저 정보 테이블을 제외한 모든 테이블의 TNAME 값을 가져온다.
SELECT TNAME FROM TAB;
TNAME 값 TEMP에 저장
for 문을 통하여 첫번째 테이블부터 돌려돌려 돌림판~
해당 textField에 입력된 문자열이 포함된 단어를 3개의 컬럼에서 search 시킨다
한자 컬럼에서 한번 select
히라가나 컬럼에서 한번 select
의미 컬럼에서 한번 select
항상 한자, 히라가나, 의미 3컬럼 출력
select kanji, hiragana, mean from [Usertable] where [column] = [textField] ;
결과값이 출력된 열부터 TNAME 값과 같이 배열에 입력
search가 다끝나면 그 배열의 leagth 만큼 for 문을 통하여 label을 4개씩 생생 후

출력 되도록

submit = >
1. 아래 컴퍼넌트 visible true
2. 아이디가 동일하고, 품사가 같은 열을 쿼리
3. 배열생성 2차원 배열 {{"문제 형식","문제","답","o or x"},{}....}

===========================

문제 형식 no 1
	한자 -> 히라가나 맞춰라
문제 형식 no 2
	히라가나 -> 한자 맞춰라
문제 형식 no 3
	한자 -> 뜻
문제 형식 no 4
	히라가나 -> 뜻
문제 형식 no 5
	뜻 -> 한자
문제 형식 no 6
	뜻 -> 히라가나

예외 ) 한자가 없는 단어는 어떻게 처리 할 것인가
문제 형식은 랜덤으로 설정

===========================

4. 첫번째 문제 lblQuiz 에 입력
5. 총 몇 문제에서 몇 번째 문제인지 체크
6. 보기를 넣을 배열 생성
7. 보기를 현재 푸는 문제를 제외하고 문제 형식에 따라 랜덤으로 3개 가져와 배열에 넣음
8. 배열의 순서를 한번 섞어줌
9. 그리고 라디오 박스 레이블에 순서대로 넣어줌
10. 사람이 next를 누르면 지금 현재 있는 라디오 박스의 레이블 getText() 받아 답과 맞는지 체크 후
정답이면 x를 o로 바꿔주고 그다음 배열 인덱스로 레이블 값들을 전환
11. 계속 next 하다가 첫 번째 배열의 길이 만큼 넥스트를 하게되면 아래 컴퍼넌트들이 visible false로 전환
12. 그리고 배열속의 o, x 중 o의 갯 수 나누기 / 사용자가 처음 입력한 숫자 * 100 하여 score에 입력

조금의 생각의 변화

- 구지 한 문장씩 데이터 베이스에서 뽑아와서 배열에 넣을 필요가 없다
사용자가 입력한 갯 수만큼 데이터 베이스에서 랜덤으로 출력한뒤 출력한 값을 배열에 입력해 넣으면 된다

 = > 문제 배열 완성

- data.cntQuiz 에 해당하는 문제의 보기를 만들어야 한다
- data.cntQuiz의 히라가나 값과 뜻 값을 where 절에서 겹치치 않도록 걸러주면서 랜덤으로 3가지 돌려서 뽑는다
그리고 해당 3가지 값을 4칸 짜리 배열에 넣어주고 마지막 칸에는 data.cntQuiz 의 값을 넣어준다

- 그리고 순서를 섞는다

바로 문제 유형을 랜덤으로 돌려서 거기에 맞도록 값을 준다.
문제 유형에 맞게 문제 값과, 그 보기 값을 꺼내서 입력
- 문제 유형은 한자가 없는 경우에는 한자가 포함된 문제 유형은 피한다.

끝

